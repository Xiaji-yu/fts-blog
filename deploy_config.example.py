# Deployment credentials template.
# Copy this file to deploy_config.py and fill in the real values.
# deploy_config.py is gitignored and must NEVER be committed.
HOST = 'your-server.example.com'
USER = 'your-user'
PASS = 'your-password'
ROOT_PASS = None  # optional: root/su password if required
