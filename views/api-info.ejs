<%- include('partials/header', { title: title, noSplash: true }) %>

<div id="apiInfoLoader" class="api-loader" aria-hidden="true">
  <div class="api-loader-inner">
    <div class="api-loader-title">API 文档</div>
    <div class="api-loader-sub">INTERFACE DOCUMENTATION · LOADING</div>
  </div>
</div>

<div class="title-block">
  <h1 class="main-title"><%= config.api.pageTitle %></h1>
  <p class="sub-title-en"><%= config.api.pageTitleEn %></p>
</div>

<hr class="divider">

<div class="spec-block">
  <p><span class="label">接口</span> <strong><%= config.api.systemLabel %></strong> · 基础URL <strong><%= config.api.baseUrl %></strong></p>
</div>

<div class="description" style="text-align: left; max-width: 800px;">

  <h2 style="font-size: 1.2rem; font-weight: 600; margin: var(--spacing-lg) 0 var(--spacing-sm);">
    <%= config.api.sections.publicEndpoints %>
  </h2>

  <table class="table">
    <thead>
      <tr>
        <th style="width: 80px;">方法</th>
        <th>端点</th>
        <th>描述</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>GET</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/posts</code></td>
        <td>获取已发布的文章列表（分页）</td>
      </tr>
      <tr>
        <td><strong>GET</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/posts/:slug</code></td>
        <td>根据 slug 获取单篇文章</td>
      </tr>
      <tr>
        <td><strong>GET</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/tags</code></td>
        <td>获取所有标签</td>
      </tr>
    </tbody>
  </table>

  <h2 style="font-size: 1.2rem; font-weight: 600; margin: var(--spacing-lg) 0 var(--spacing-sm);">
    <%= config.api.sections.adminEndpoints %>
  </h2>

  <table class="table">
    <thead>
      <tr>
        <th style="width: 80px;">方法</th>
        <th>端点</th>
        <th>描述</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>POST</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/auth/login</code></td>
        <td>管理员登录</td>
      </tr>
      <tr>
        <td><strong>POST</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/auth/logout</code></td>
        <td>管理员登出</td>
      </tr>
      <tr>
        <td><strong>POST</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/posts</code></td>
        <td>创建新文章</td>
      </tr>
      <tr>
        <td><strong>PUT</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/posts/:id</code></td>
        <td>更新文章</td>
      </tr>
      <tr>
        <td><strong>DELETE</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/posts/:id</code></td>
        <td>删除文章</td>
      </tr>
      <tr>
        <td><strong>POST</strong></td>
        <td><code style="font-family: var(--font-mono); font-size: 0.85rem;">/api/upload</code></td>
        <td>上传图片 (multipart/form-data)</td>
      </tr>
    </tbody>
  </table>

  <h2 style="font-size: 1.2rem; font-weight: 600; margin: var(--spacing-lg) 0 var(--spacing-sm);">
    <%= config.api.sections.authentication %>
  </h2>

  <div class="spec-block" style="text-align: left;">
    <p>管理接口需要 session-based 认证。</p>
    <p style="margin-top: 0.5rem;">
      1. 登录: <strong>POST /api/auth/login</strong><br>
      请求体: <code style="font-family: var(--font-mono);">{ "username": "<%= config.admin.defaultUsername %>", "password": "YOUR_PASSWORD" }</code><br>
      响应: <code style="font-family: var(--font-mono);">{ "message": "...", "user": {...}, "csrfToken": "..." }</code>
    </p>
    <p style="margin-top: 0.5rem;">
      2. 所有写操作（POST/PUT/DELETE）除登录外，都必须在请求头携带 CSRF 令牌:<br>
      <code style="font-family: var(--font-mono);">X-CSRF-Token: &lt;csrfToken&gt;</code><br>
      令牌来自登录响应中的 <code style="font-family: var(--font-mono);">csrfToken</code> 字段，
      或 <strong>GET /api/auth/csrf</strong>（需要同一会话 cookie）。
    </p>
    <p style="margin-top: 0.5rem; color: var(--muted);">
      浏览器表单场景使用隐藏字段 <code style="font-family: var(--font-mono);">_csrf</code> 提交亦可。
    </p>
  </div>

  <h2 style="font-size: 1.2rem; font-weight: 600; margin: var(--spacing-lg) 0 var(--spacing-sm);">
    <%= config.api.sections.example %>
  </h2>

  <div class="spec-block" style="text-align: left; background: var(--bg-alt); padding: var(--spacing-md); border: 1px solid var(--border);">
    <p style="margin-bottom: 0.5rem;"># 1. 获取所有文章（公开）</p>
    <p>curl <%= config.api.baseUrl.replace(/\/api$/, '') %>/api/posts</p>
    <br>
    <p style="margin-bottom: 0.5rem;"># 2. 登录并保存 Cookie 与 CSRF 令牌</p>
    <p>curl -c cookies.txt -X POST <%= config.api.baseUrl %>/auth/login \<br>
    &nbsp;&nbsp;-H "Content-Type: application/json" \<br>
    &nbsp;&nbsp;-d '{"username":"<%= config.admin.defaultUsername %>","password":"YOUR_PASSWORD"}'</p>
    <br>
    <p style="margin-bottom: 0.5rem;"># 3. 创建新文章（携带 Cookie + CSRF 头）</p>
    <p>curl -b cookies.txt -X POST <%= config.api.baseUrl %>/posts \<br>
    &nbsp;&nbsp;-H "Content-Type: application/json" \<br>
    &nbsp;&nbsp;-H "X-CSRF-Token: &lt;上一步响应中的 csrfToken&gt;" \<br>
    &nbsp;&nbsp;-d '{"title":"测试","slug":"test","content":"内容"}'</p>
  </div>

</div>

<%- include('partials/footer') %>
